apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: delivery-production
resources:
  - ../../base

# This is intentionally a render-only template. A production overlay additionally
# requires provider-specific ingress/TLS, workload identity, NetworkPolicy,
# PDB/capacity profile, data-plane and observability integration.
