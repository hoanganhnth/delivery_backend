apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: delivery-staging
resources:
  - ../../base

# This is intentionally a render-only template. Copy this directory to a
# private environment overlay, patch all *.example.invalid endpoints, image
# digests, ConfigMap values and secret-provider resources before applying.
