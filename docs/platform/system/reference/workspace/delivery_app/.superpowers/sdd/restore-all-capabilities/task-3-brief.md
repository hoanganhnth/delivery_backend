### Task 3: Flash Sale and promotion restoration

- Add the Flutter `features/flash_sale` module with repository/use-case/ViewModel/view tests, home/menu entry points, countdown, loading/error/empty states, and server-authoritative `flashSaleItemId` through cart, preview, and order creation.
- Reconcile public catalog contracts and guarded reservation/checkout behavior in `backend_delivery`; keep reservation default-off until sandbox proof.

